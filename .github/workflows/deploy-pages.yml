# Disabled: Saravá now deploys on Vercel (no GitHub Pages / docs/ export).
# Kept as a no-op so old workflow references do not surprise anyone.
name: Deploy Pages (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "GitHub Pages deploy is retired. Use Vercel."
